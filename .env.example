# oobe-ace-agent — Environment Variables (DEVELOPMENT ONLY)
# ⚠️  NEVER use this in production. See SECURITY.md for HSM/vault recommendations.

# Solana RPC endpoint
RPC_URL=https://api.mainnet-beta.solana.com

# PRIVATE KEY — READ SECURITY.md BEFORE USING
# For production: use AWS KMS / GCP Secret Manager / HSM instead
# For local dev only:
SOLANA_PRIVATE_KEY=

# x402 guard endpoint
X402_GUARD_URL=

# SAP registry address
SAP_REGISTRY_ADDRESS=
